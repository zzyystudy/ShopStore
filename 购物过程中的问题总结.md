## 下单幂等校验

在下单的时候会生成一个唯一下单请求 标识一次下单请求 防止网络波动用户疯狂点击下单。 

疯狂点击下单 每次点击这个唯一的下单请求号不变 还是那个唯一的请求号，为什么呢?这个唯一下单请求编号不是点击按钮的时候产生的，而是进入下单界面就会自动生成一个请求编号，后端识别到多次下单是同一个下单请求，就直接屏蔽不做处理，同时为了防止这个唯一下单标识被滥用

## 订单邮箱存储

我设计的系统是下单时预留邮箱地址和一个安全密码 每个订单都会有一个安全密码 查询订单的时候通过安全密码查询，密码在数据库中直接使用bcrypt存储，邮箱怎么存储呢 不能密文存储，这里我们使用aes(对称加密算法)加密，可以解密发货，这个aes加密为了防止重放攻击所以要加一个nonce值(类似于随机盐值，但是盐值是为了防止彩虹表攻击的，这个nonce是防止重放攻击的)，总的来说就是aes加密需要一个密钥和一个nonce加密，然后每个订单由于nonce不一样得到最后加密结果不一样，所以我们查询订单的时候不能通过这个aes加密之后的结果查询订单，所以我们需要再存储一个hash后的结果每次hash结果都一样，我们查询订单通过这个hash进行查询。然后我们还要存储一个version(密钥版本)，这个是因为企业每一年都要更换密钥，解密算法根据这个字段来判断用哪个密钥进行解密。

![img](file:///C:\Users\zzyy-\AppData\Local\Temp\QQ_1787390057465.png)

## 下单到付款到发货的整个流程在 codex写的文档中